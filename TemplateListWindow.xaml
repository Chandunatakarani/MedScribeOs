<Window x:Class="MedScribeOS.TemplateListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Note Templates · MedScribeAI"
        Height="600" Width="640"
        Background="{StaticResource BgDarkBrush}"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📋 Note Templates"
                   Foreground="{StaticResource TextPrimaryBrush}" FontSize="20" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" x:Name="SubHeader" Margin="0,4,0,0"
                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>

        <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="ListPanel"/>
        </ScrollViewer>

        <DockPanel Grid.Row="3" Margin="0,16,0,0" LastChildFill="False">
            <Button x:Name="BtnNew" Content="＋ New Template" Style="{StaticResource PrimaryButtonStyle}"
                    DockPanel.Dock="Left" Click="BtnNew_Click"/>
            <Button x:Name="BtnClose" Content="Close" Style="{StaticResource GhostButtonStyle}"
                    DockPanel.Dock="Right" Click="BtnClose_Click"/>
        </DockPanel>
    </Grid>
</Window>
